# Copy this to docker.env and fill values when running with Docker or Docker Compose
# Keep this file out of source control if adding secrets

# Connector mode: LOCAL or BIGQUERY
connector.mode=LOCAL
server.port=8080
# App API key used to protect /bq endpoints
app.api.key=replace_with_secure_key

# Optional - Google service account JSON (either mount file and use GOOGLE_CREDENTIALS_PATH, or provide JSON here)
# GOOGLE_CREDENTIALS_JSON={...}
# If you prefer to mount a JSON file, set: GOOGLE_CREDENTIALS_PATH=/secrets/gcp.json
# Example for Metabase embedings
METABASE_SITE_URL=https://metabase.example.com
METABASE_SECRET_KEY=replace-with-metabase-secret
METABASE_DASHBOARD_ID=1
# Optional BigQuery project settings (Only used when connector.mode=BIGQUERY)
GCP_PROJECT_ID=replace-your-project-id
GCP_BIGQUERY_DATASET=your_dataset
GCP_BIGQUERY_TABLE=students
